<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/item_cover"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:layout_marginBottom="5dp"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <ImageView
        android:id="@+id/item_avatar"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginLeft="15dp"
        android:layout_marginBottom="25dp"
        android:layout_marginTop="15dp"
        android:background="#DDD"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/item_detail"
        app:layout_constraintBottom_toTopOf="@id/item_cover"/>

    <TextView
        android:id="@+id/item_detail"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:ellipsize="end"
        android:maxLines="4"
        android:textColor="#555"
        android:textSize="14sp"
        app:layout_constraintLeft_toRightOf="@id/item_avatar"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/item_avatar"
        tools:text="A radically inclusive, just and loving community mobilized to alleviate suffering and break the cycles of poverty and marginalization."/>

    <TextView
        android:id="@+id/item_nick"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:textColor="#444"
        android:textSize="15sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="@id/item_avatar"
        app:layout_constraintRight_toRightOf="@id/item_avatar"
        app:layout_constraintTop_toBottomOf="@id/item_avatar"
        app:layout_constraintBottom_toTopOf="@id/item_cover"
        android:layout_marginBottom="5dp"
        tools:text="Jimmy"/>

</android.support.constraint.ConstraintLayout>